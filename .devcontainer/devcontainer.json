{
  "name": "dotnet 10 (no Playwright)",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csharp"
      ]
    }
  },
  "postCreateCommand": "bash -lc 'set -e; dotnet restore || true; if [ -f \"dotnet-tools.json\" ]; then dotnet tool restore || true; fi'",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "remoteUser": "vscode"
}